version: v1.0
name: Complex pipeline without dependencies

agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

blocks:
  - task:
      jobs:
        - commands:
            - echo "Hello world!"
promotions:
  - name: push artifacts
    pipeline_file: push-artifacts.yml
  - name: pull artifacts
    pipeline_file: pull-artifacts.yml
