version: v1.0
name: Promo
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
after_pipeline:
  task:
    jobs:
      - name: Reports
        commands:
          - test-results gen-pipeline-report
blocks:
  - name: Build
    task:
      jobs:
        - commands:
          - checkout
          - test-results publish test/$(ls test | shuf -n 1)